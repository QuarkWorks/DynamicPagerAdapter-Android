<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quarkworks.dynamicpageradapter_android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".single_view_type.SinglePagerActivity"
            android:label="@string/single_view_type" />

        <activity android:name=".multi_view_type.MultiPagerActivity"
            android:label="@string/multi_view_type" />

    </application>

</manifest>